---
import AlbumCard from "../components/AlbumCard.astro";
import siteConfigJson from "../config/site.json";
import albumDataJson from "../data/albums.json";
import Layout from "../layouts/Layout.astro";
import type { AlbumData } from "../types/album";
import type { SiteConfig } from "../types/config";

const albumData = albumDataJson as AlbumData;
const { albums } = albumData;
const siteConfig = siteConfigJson as SiteConfig;
const { homepage } = siteConfig;
---

<Layout>
  <div
    class="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50/30 to-cyan-50/20 dark:from-slate-900 dark:via-blue-950/30 dark:to-cyan-950/20"
  >
    <!-- Header -->
    <header
      class="bg-gradient-to-r from-white/90 via-purple-50/50 to-blue-50/30 dark:from-slate-800/90 dark:via-purple-950/50 dark:to-blue-950/30 backdrop-blur-lg border-b border-slate-200/60 dark:border-slate-700/60 shadow-lg"
    >
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="text-center">
          <h1
            class="text-6xl font-black bg-gradient-to-r from-purple-600 via-blue-600 to-cyan-500 dark:from-purple-400 dark:via-blue-400 dark:to-cyan-300 bg-clip-text text-transparent hover:scale-105 transition-transform duration-300 inline-block"
          >
            {homepage.title}
          </h1>
          <p
            class="mt-4 text-xl text-slate-600 dark:text-slate-400 font-medium"
          >
            {homepage.subtitle}
            <span
              class="font-bold bg-gradient-to-r from-purple-600 to-blue-600 dark:from-purple-400 dark:to-blue-400 bg-clip-text text-transparent"
              >{albums.length}</span
            >
            {albums.length === 1 ? "collection" : "collections"}
          </p>
        </div>
      </div>
    </header>

    <!-- Main content -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      {
        albums.length === 0 ? (
          <div class="text-center py-12">
            <svg
              class="mx-auto h-24 w-24 text-gray-400"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"
              />
            </svg>
            <h3 class="mt-4 text-lg font-medium text-gray-900 dark:text-gray-100">
              No albums yet
            </h3>
            <p class="mt-2 text-gray-600 dark:text-gray-400">
              Add photos to the{" "}
              <code class="bg-gray-100 dark:bg-gray-800 px-2 py-1 rounded">
                public/albums/
              </code>{" "}
              directory
            </p>
            <p class="mt-2 text-sm text-gray-500 dark:text-gray-400">
              See{" "}
              <code class="bg-gray-100 dark:bg-gray-800 px-2 py-1 rounded">
                public/albums/README.md
              </code>{" "}
              for instructions
            </p>
          </div>
        ) : (
          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
            {albums.map((album) => (
              <AlbumCard
                id={album.id}
                title={album.title}
                description={album.description}
                coverPhoto={album.coverPhoto}
                coverPhotoThumbnail={album.coverPhotoThumbnail}
                photoCount={album.photoCount}
              />
            ))}
          </div>
        )
      }
    </main>
  </div>
</Layout>
