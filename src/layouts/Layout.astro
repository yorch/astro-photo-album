---
import "../styles/global.css";
import ThemeSwitcher from "../components/ThemeSwitcher.astro";
---

<!doctype html>
<html
  lang="en"
  x-data="{ theme: $persist('light').as('app-theme') }"
  x-bind:class="theme === 'auto' ? (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light') : theme"
  x-init="
    if (theme === 'auto') {
      const darkModeQuery = window.matchMedia('(prefers-color-scheme: dark)');
      darkModeQuery.addEventListener('change', (e) => {
        $el.classList.toggle('dark', e.matches);
        $el.classList.toggle('light', !e.matches);
      });
    }
  "
>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Photo Album</title>
  </head>
  <body
    class="bg-gray-50 dark:bg-slate-900 text-slate-900 dark:text-slate-100 transition-colors duration-200"
  >
    <ThemeSwitcher />
    <slot />
    <script>
      import intersect from "@alpinejs/intersect";
      import persist from "@alpinejs/persist";
      import Alpine from "alpinejs";

      // Register Alpine plugins
      Alpine.plugin(intersect);
      Alpine.plugin(persist);

      // Initialize Alpine.js
      window.Alpine = Alpine;
      Alpine.start();
    </script>
  </body>
</html>

<style>
  html,
  body {
    width: 100%;
    height: 100%;
    margin: 0;
  }
</style>
